@extends('layouts.app') @section('content')<h2>Manage Users</h2><table class="table"><thead><tr><th>ID</th><th>Email</th><th>Role</th><th>Active</th><th>Action</th></tr></thead><tbody>@foreach($users as $u)<tr><td>{{ $u->id }}</td><td>{{ $u->email }}</td><td>{{ $u->role }}</td><td>{{ $u->active ? 'Yes' : 'No' }}</td><td><form method="post" action="/users/{{ $u->id }}/toggle">@csrf<button class="btn btn-sm btn-warning">Toggle</button></form></td></tr>@endforeach</tbody></table>{{ $users->links() }}@endsection
